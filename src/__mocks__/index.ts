// Mock handlers (axios-mock-adapter). Only loaded when VITE_USE_MOCKS=true.
// The employee/Home demo mocks were retired with the DBChat migration; add
// DBChat mocks here if a backend-less demo mode is needed.
export {}
